# CLAUDE.md — sensuLP

## このリポジトリについて

株式会社橘一「お誂え扇子 特別受注」のランディングページ。

- `index.html` … LP 本体（単一ファイル完結）
- 公開範囲 … **public**（GitHub 上で誰でも閲覧できます）

---

## ⚠️ 最重要ルール：このリポジトリは公開されています

`wai891/sensuLP` は public です。ここに置いたファイルは、
**ブランチを分けても、main にマージしていなくても、URL を知る誰もが読めます。**

「まだマージしていないから非公開」ではありません。push した時点で公開です。

### 置いてよいもの

- 扇子LP の HTML / CSS / JS / 画像
- 公式サイトで既に公開している情報（所在地・代表電話・公開料金など）

### 置いてはいけないもの（private リポジトリへ）

- 社内向けの業務ノウハウ・運用ルール・返信テンプレート
- 採用案件の経緯、社内メールの実例
- 社員・役職者の氏名を含む署名ブロックや担当振り分け表
- 未公開の料金・原価・仕入条件
- 顧客に関する記録（匿名化済みでも、案件の経緯が分かるもの）
- API キー、トークン、パスワード、`.env` の類

判断に迷ったら **置かずに、しげるさんに確認する。**

---

## 作業ルール

- 変更は `claude/...` ブランチで行い、`main` へ直接 push しない
- ファイルを追加するときは、上の「置いてはいけないもの」に当たらないか毎回確認する
- コミット前に `git status` と `git diff --cached --stat` で、
  意図しないファイルが混ざっていないか確認する
- `Projects/` `HANDOFF_*.md` は `.gitignore` で除外済み。
  `git add -f` で強制追加しないこと

---

## 橘一の業務知識を扱うときの約束

- 着物の専門用語・寸法・料金は業界の実務が絶対。一般論で補わない。分からなければ聞く
- 社外に出す文面に固有の番号・契約情報・担当者名を書くときは、記憶や過去メモを根拠にしない。
  一次資料（請求書・実メール）での確認を促し、確認できないものは空欄にしておく
- 確かめていないことを、確かめたことにしない。推測で埋めた箇所は「これは推測です」と明示する
- 「動かない」「届かない」系は、原因を推測する前にまず経路を1つに確定する
  （誰が・どのシステムから・どの設定で）

---

## index.html の技術メモ

- Tailwind は CDN 版（`cdn.tailwindcss.com`）、React は UMD 版
- JSX は `@babel/standalone` によるブラウザ実行（`<script type="text/babel">`）
- この構成は初回表示が重い。表示速度を詰める段になったらビルド導入を検討する
- 画像は `ImageWithFallback` コンポーネントが候補ファイル名を順に試す作り
